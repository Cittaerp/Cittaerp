@model  CittaErp.Models.vw_genlay
@{
    ViewBag.Title = "inter_company";
    ViewBag.action_flag = "Edit";
    string plabel = ViewBag.plabel;
}
@*Html.Partial("menu")*@
<div class="container-fluid">
    <form class="form-horizontal" role="form" method="post" enctype="multipart/form-data">
        <h3>Inter Company Maintenance</h3>
        @Html.ValidationSummary(true)

        @Html.Partial("_listupdel")

        @Html.Partial("Inter_coy");
        @Html.HiddenFor(m => m.vwstring0)
        @Html.Hidden("id_xhrt", "K")
    </form>
</div>


@section Scripts {
    <script>

        $("#id_delete").click(function () {
            var $form = $(this).closest('form');
            bootbox.confirm("Are you sure you want to delete?", function (a) {
                if (a) {
                    $("#id_xhrt").val("D");
                    $form.trigger("submit");
                }
                else {
                    $("#id_xhrt").val("X");
                }
            });
        });
    </script>
}