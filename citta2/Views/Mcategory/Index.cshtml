@model  IEnumerable<CittaErp.Models.vw_genlay>
@{
    ViewBag.Title = "category";   
}
@*Html.Partial("menu")*@
<h3>Account Category Listing</h3>
@Html.ActionLink("Add New", "Create", "", new { @class = "btn btn-primary", @style = "color:white" })

        <table class="myTable1 display">

    <thead>
        <tr>
            <th>Category Id</th>
            <th>Description</th>
            <th>Report Group</th>
            <th>Report Sequence</th>
            <th>Status</th>
            <th>Edit</th>
            <th>Delete</th>
        </tr>
    </thead>
    <tbody>

        @foreach (var item in Model)
        {
            <tr>
                <td>@item.vwstring4</td>
                <td>@item.vwstring0</td>
                <td>@item.vwstring1</td>
                <td>@item.vwint1</td>
                <td>@item.vwstring3</td>
                <td>
                    @Html.ActionLink("E", "Edit", new { key1 = @item.vwstring4}, new { @class = "btn btn-warning btn-xs glyphicon glyphicon-pencil" })
                </td>
                <td><span class="btn btn-danger btn-xs deleteitem" data-keyg="@item.vwstring4"> <i class="glyphicon glyphicon-trash "></i> </span></td>
            </tr>
        }

    </tbody>

</table>
        @Html.Hidden("id_xhrt", "K")
        
<div id="loader2" data-request-ajax2="@Url.Action("delete_list")"></div>

@section Scripts {
    <script>

    </script>
}