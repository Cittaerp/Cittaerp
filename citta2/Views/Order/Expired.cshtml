@model  IEnumerable<CittaErp.Models.vw_genlay>
@{
    ViewBag.Title = "sales";
}
@*Html.Partial("menu")*@
<h3>Expired Quote Reactivation</h3>
@*@Html.ActionLink("Add New", "CreateHeader", "", new { @class = "btn btn-primary", @style = "color:white" })*@


<table class="myTable1 display">

    <thead>
        <tr>
            <th>Quote reference</th>
            <th>Customer</th>
            <th>Currency code</th>
            <th>Quote amount</th>
            <th>Quote date</th>
            <th>Quote expiration date</th>
            <th>Void</th>
            <th>Reactivate</th>
        </tr>
    </thead>
    <tbody>

        @foreach (var item in Model)
        {
            <tr>
@if (Convert.ToInt32(item.vwdate2.ToString("yyyyMMdd")) > Convert.ToInt32(item.vwstring4)) {
                <td>@item.vwstring0</td>
                <td>@item.vwstring1</td>
                <td>@item.vwstring2</td>
                <td class="numformat">@item.vwdecimal0</td>
                <td>@item.vwstring3.Substring(6, 2)/@item.vwstring3.Substring(4, 2)/@item.vwstring3.Substring(0, 4)</td>
                <td>@item.vwstring4.Substring(6, 2)/@item.vwstring4.Substring(4, 2)/@item.vwstring4.Substring(0, 4)</td>
                <td><span class="btn btn-outline btn-xs voiditem" data-keyg="@item.vwint0" style="color:red"> <i class="glyphicon "></i>Void </span></td>  
                <td><span class="btn btn-danger btn-xs activateitem" data-keyg="@item.vwint0"> <i class="glyphicon "></i>Reactivate </span></td>    
                @*@if (Convert.ToInt32(item.vwstring3) < Convert.ToInt32(item.vwstring4)) {
    <td>
        @Html.ActionLink("C", "QuoteConvert",null, new { key1 = @item.vwint0, key2 = @item.vwstring0, headtype = "without" }, new { @class = "btn btn-warning btn-xs glyphicon glyphicon-pencil" })
    </td>
    }
    else
    {
       <td><span  style="color:red"> Expired</span></td>
    }
    @if (Convert.ToInt32(item.vwstring3) < Convert.ToInt32(item.vwstring4))
    {
        <td>
            @Html.ActionLink("C", "QuoteConvert", null, new { key1 = @item.vwint0, key2 = @item.vwstring0, headtype = "with" }, new { @class = "btn btn-warning btn-xs glyphicon glyphicon-pencil" })
        </td>
    }
    else
    {
        //<td><span style="color:red"> Expired</span></td>
        <td><span class="btn btn-danger btn-xs voiditem" data-keyg="@item.vwint0"> <i class="glyphicon "></i>Void </span></td>
    }*@
}
            </tr>
        }

    </tbody>

</table>
@Html.Hidden("id_xhrt", "K")


<div id="loader21" data-request-ajax21="@Url.Action("void_list")"></div>
<div id="loader20" data-request-ajax20="@Url.Action("reactivate_list")"></div>

@section Scripts {
    <script>


    </script>
}