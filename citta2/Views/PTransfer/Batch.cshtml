@model anchor1.Models.vw_collect 
@{
    ViewBag.Title = "";
}


@using (Html.BeginForm("Batch", "PTransfer", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    @Html.Partial("_ProcessBtn")

    <fieldset>
        <legend>tab_allow</legend>
        
    <div class="container-fluid table-edit">
        <div class="row">
            <div class="col-sm-3 col-sm-text-right">Document File List</div>
            <div class="col-sm-8"> 
                <input type="file" id="filename" name="filename" />
            </div>
        </div>
    </div>
                
   </fieldset>
}
