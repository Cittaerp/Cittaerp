@model anchor1.Models.vw_collect 


@using (Html.BeginForm()) {

    @Html.AntiForgeryToken()    
    @Html.ValidationSummary(true)

    @Html.Partial("_ProcessBtn")

    <fieldset>
        <div class="container-fluid table-edit" >
        
            <div class="row">
                <div class="col-sm-3 col-sm-text-right">@Model.ws_string1</div>
                <div class="col-sm-3 ">
                    @Html.DropDownList("ws_string4",ViewBag.codequery as SelectList,"",new { @class = "form-control mypadding " })
                </div>
                <div class="col-sm-1 text-center">To</div>
                <div class="col-sm-3 ">
                    @Html.DropDownList("ws_string5", ViewBag.codequery as SelectList,"",new { @class = "form-control mypadding " })
                </div>
        </div>
    @if(! string.IsNullOrWhiteSpace(Model.ws_string2 ))
    {
             <div class="row">
                <div class="col-sm-3 col-sm-text-right"> @Model.ws_string2 </div>
                <div class="col-sm-3 ">
                    @Html.DropDownList("ws_string6",ViewBag.numquery as SelectList,"",new { @class = "form-control mypadding " } )
                </div>
                <div class="col-sm-1 text-center">To</div>
                <div class="col-sm-3 ">
                    @Html.DropDownList("ws_string7", ViewBag.numquery as SelectList,"",new { @class = "form-control mypadding " })
                </div>           
            </div>
    }

        </div>

        @Html.HiddenFor(m => m.ws_string0)
    </fieldset>
}

<div id="loading">
    <div id="loadingcontent">
        <p id="loadingspinner">
            Pls wait...
        </p>
    </div>
</div>

