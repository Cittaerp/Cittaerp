@model  CittaErp.Models.vw_genlay
@{
    CittaErp.Models.psess psess = (CittaErp.Models.psess)Session["psess"];
    //if (Model.datmode == "E")
    //{
    //    ViewBag.action_flag = "EditDetails";
    //}
    //else
    //{ ViewBag.action_flag = "CreateDetails";
    //}

    string plabel = ViewBag.plabel;
}


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    @Html.ValidationSummary()

<h3>@psess.temp1</h3>
        @Html.Partial("_TransHeader")
        @Html.Partial("Tran")


    @Html.HiddenFor(m => m.vwstring9)
    @Html.HiddenFor(m => m.vwdecimal4)
    @Html.HiddenFor(m => m.vwdecimal5)

}

<div id="loader4" data-request-ajax4="@Url.Action("trancode")"></div>
<div id="loader1" data-request-ajax1="@Url.Action("budget_cal")"></div>
<div id="loader2" data-request-ajax2="@Url.Action("delete_detail")"></div>
<div id="loader5" data-request-ajax5="@Url.Action("ac_curen")"></div>
<div id="loader6" data-request-ajax6="@Url.Action("b_amt")"></div>

@section Scripts {
    <script>
        $(document).ready(function () {

            $("#submit").hide();

            $("#hide_details").click(function () {
                $("#submit").show();
                $("#hide_details").hide();
            });

            $("#submit").click(function () {
                $("#details").show();
                $("#submit").hide();
                $("#hide_details").show();

            });

         });
    </script>
<script type="text/javascript" src="@Url.Content("~/Scripts/CittaScripts/transt.js")"></script>
}
