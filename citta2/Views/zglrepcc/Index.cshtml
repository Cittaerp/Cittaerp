@model IEnumerable<CittaErp.Models.vw_genlay >

@{
    ViewBag.Title = "Index";
}
@Html.Partial("menu")
@Html.ValidationSummary(true)
<h3>@psess.temp1</h3>
@Html.ActionLink("Add New", "Create", "", new { @class = "btn btn-primary", @style = "color:white" })



<table class="myTable1 display" >
<thead>
    <tr>
        <th>Id</th>
        <th>Name</th>
        <th>Report Type</th>
        <th>Menu Page</th>
        <th>Edit</th>
        <th>Delete</th>
    </tr>
    </thead>

    <tbody>
@foreach (var item in Model) {
    <tr>
        
         <td>
           @Html.DisplayFor(modelItem => item.vwstring0)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.vwstring1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.vwstring3)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.vwstring2)
        </td>
        <td>
            @Html.ActionLink("E", "Edit", new { jy = @item.vwstring0 }, new { @class = "btn btn-warning btn-xs glyphicon glyphicon-pencil" })
        </td>
        <td><span class="btn btn-danger btn-xs deleteitem" data-keyg="@item.vwstring0"> <i class="glyphicon glyphicon-trash "></i> </span></td>

    </tr>
    }

</tbody>
</table>

@section Scripts {

<script>


</script>

}